# 리뷰 라우팅 + **어디가 조용히 깨지는가**의 지도.
#
# 1인 레포라 소유자는 한 명이다. 그래도 이 파일을 두는 이유는 두 가지다:
#   1. 아래 경로들은 회귀가 났을 때 **테스트가 아니라 결과물의 의미**가 틀어진 자리다.
#      한 줄 바꿔도 리뷰를 요청받게 만든다.
#   2. 에이전트가 리뷰어일 때 "이 변경이 무엇을 건드리는가"를 diff 순서가 아니라
#      위험도 순서로 읽게 하는 색인이 된다.
# 체크리스트 본문은 [pull_request_template.md](pull_request_template.md).

*                                   @dxlpi

# ── 1급 규칙이 사는 곳 (바뀌면 프로젝트의 주장 자체가 바뀐다)
/slime_rag/bias.py                  @dxlpi   # 출처 편향 분리 — 평균내면 이 프로젝트의 존재 이유가 사라진다
/slime_rag/relevance.py             @dxlpi   # 드롭 사유는 M 하나뿐. 부정 후기 보존(bias_hold)
/slime_rag/relevance_rules.py       @dxlpi
/slime_rag/consolidated_view.py     @dxlpi   # CRITERIA 한 리스트가 스키마·프롬프트·화면을 동시에 움직인다
/slime_rag/llm_ops.py               @dxlpi   # 벤더 격리 · 비용/토큰 원장 (하드게이트 #3)
/slime_rag/source_links.py          @dxlpi   # 링크·임베드 정책 — 틀린 링크는 링크 없음보다 나쁘다
/sql/schema.sql                     @dxlpi   # 컬럼 하나가 db·index·search·consolidated_view 로 파급

# ── 컨텍스트 계층 (썩으면 에이전트가 있는 걸 다시 만든다)
/CLAUDE.md                          @dxlpi
/ARCHITECTURE.md                    @dxlpi
/MEMORY.md                          @dxlpi
**/CLAUDE.md                        @dxlpi
/docs/adr/                          @dxlpi   # 결정 기록 — 고치는 게 아니라 새 ADR 로 뒤집는다
/.github/context_claims.json        @dxlpi   # 부재 주장 레지스트리
/.github/scripts/                    @dxlpi

# ── 게이트 자체 (느슨해지면 나머지 전부가 조용히 통과한다)
/.github/workflows/                 @dxlpi
/.githooks/                         @dxlpi
/eval/                              @dxlpi
/evals/gold/                        @dxlpi   # 사람 검수 골드 — 라벨 변경은 재검수 없이는 금지

# ── 디자인 대조가 걸린 곳
/web/src/components/kds/            @dxlpi   # 디자인 시스템 사본 — 편집 금지
/web/src/styles/kds/                @dxlpi   # 토큰 byte-for-byte 사본
/web/src/screens/SlimeSearch.tsx    @dxlpi   # 인라인 style 값이 목업과 1:1 (0.025% 픽셀 차)
