# MEMORY — 도메인 암묵지 & 의사결정 근거

코드만 봐서는 알 수 없는 슬라임 도메인 규칙과 "왜 이렇게 했나"를 레포 안에 외부화한다.
구조적 결정은 [docs/adr/](docs/adr/) 의 번호 매긴 레코드로, 반복 적용되는 도메인 규칙은
아래에 둔다. 아키텍처 흐름은 [ARCHITECTURE.md](ARCHITECTURE.md).

## 마켓 KB 구조
- repo 포함은 **데모 13개**만: `data/slime_market_kb_demo.json`(봄·웨이즈·진통제·베이퍼·머머·
  푸딩·빈짱·모모네·연찌·캐치·예찬·지나·늪지). 전체 118개는 미포함(필요 시 사용자 제공).
- 최상위 `{slime_types[], markets[]}`. 마켓 필드 = `market, handle, handles_alt[], market_word,
  choseong, choseong_aliases[], aliases[], products[]`. `products[]` 는 비어있음(1층 미시드).
- **머머는 계정 2개 병합**: 대표 `from.murmurslime`, 보조는 `handles_alt` 의 `murmurslime`.
- **Note:** 초성 충돌 12그룹(ㅁㅁ·ㅇㅇ·ㄴㅈ 등)은 전체 KB 기준 — 데모 13개엔 충돌 없음(개체연결 참조).

## 통제 어휘 (도메인)
- **슬라임 종류(TYPE_ENUM, 1층)**: 폼볼, 촉감류(점토), 디폼, 난사, 눈꽃, 지글리, 크런치,
  빈백, 클라우드, 샤베트, 클리어, 버글리, 젤라또, 빨대, 라이스볼, 수수깡, 자바칩, 스티로폼,
  **납작블럭**(2026-08-09 사용자 추가) (확장 가능).
  **두 갈래다**(사용자 규칙 2026-08-09) — **내장물형**(폼볼·디폼·빈백·빨대·라이스볼·수수깡·
  자바칩·난사·스티로폼·납작블럭): 머리말 '6미리 디폼 내장' 줄에 적히고 `beads` 에도 같은 말이 든다 ·
  **베이스 성격어**(촉감류(점토)·클리어·크런치·버글리·지글리·샤베트·젤라또·클라우드·눈꽃):
  베이스의 제형·투명도·소리. **내장물 우선이되 성격어도 성립하면 둘 다 적고**(성격어를 앞에)
  `slime_type` 은 콤마결합해 저장한다 — 예) `크런치, 디폼` · `클리어, 수수깡` · `빈백, 폼볼`.
  ⚠️ '점토'가 풀조합 나열에 끼어 있는 건 대개 소량이라 **촉감류가 아니다**(예: 빠코볼=폼볼).
  ⚠️ **`납작블럭`은 표기가 갈린다** — 머머·빈짱·예찬은 `블럭`, 베이퍼·진통제는 `블록`
  (실측 2026-08-09, 14마켓 피드 원문: 블럭 11게시물 · 블록 3게시물). 통제어휘 표기는
  **`납작블럭` 하나**로 접는다. 그리고 **`~블럭`이라고 다 종류가 아니다** — 네모블럭(17)·
  샌드블럭(10)·원형블럭(4)·사각블럭(4)·하트블럭(2)·칸블럭(7)은 통제어휘 **밖**이라 종류로
  적지 않고 `beads` 에만 들어간다. 통제어휘는 **어휘의 문제**라 빈도로 안 갈린다 — 새 종류어는
  사람이 승격한다(같은 이유로 `pipeline._type_tag_forms` 가 KB `slime_types` 를 읽는다).
- **질감 서술어(FEEL_VOCAB, 2층)**: 말랑·말캉·쫀득·퐁신·폭닥·크리미·로션크리미·얄랑·매트·
  빳빳·텐션감있는·흐물거리는·쳐지는·흐름성있는 + `~같은` 비유 + 신규어(feel_other).

## 속성 분류 규칙 (사람이 가르친 것)
- **Why:** "걀걀거림"은 **sound**(소리)지 질감이 아니다 — texture 로 태깅하면 오답.
- **Why:** "지속력"은 제품 속성이지 **배송(shipping)** 과 무관 — 섞지 말 것.
- **주의:** 향 불일치/소스 갭은 LLM 이 아니라 조인·집계에서 계산(`consolidated_view.py`).

## 2층 관련성 3축 M/Q/E (ADR-0006)
- **절대 축 vs 쿼리 조건부 축**: `label.M/Q/E` 는 **텍스트의 속성**이라 타깃이 바뀌어도 같아야 하고,
  `label.keep`+`collected_for` 는 **타깃에 관한 것이냐**라 같은 텍스트라도 달라진다.
  같은 텍스트인데 keep 이 다른 6건은 버그가 아니라 AC3 하드 네거티브 **자산**이다.
- **DROP 사유는 `M`(갤 메타/뉴스 위젯) 하나뿐.** E 음성은 드롭이 아니라 순위 꼬리다.
  **`Q` 는 순수 관측 축** — 정렬 키 `(e_bucket, bias_hold, topic_score)` 에 안 들어가고, 색인·요약에도
  도달하지 않는다(로그와 `RawReview.meta` 까지). 질문글이 밀리는 건 E=0 때문이지 Q 때문이 아니다.
  예산 초과분은 `unprocessed`(미처리)로 따로 센다 — 드롭과 섞으면 침묵 절단이 된다.
- **부정 감성은 `E=0` 이어도 후보 유지**(`bias_hold`). 부정 후기 1건 손실 > 헛호출 10회 — 소스 편향이 1급이라서.
- **한국어 증거성 표지**: `-더라`/`-던데`=직접 지각(1인칭), `-다더라`/`-다고`/`-대서`=전언(남의 경험).
  둘을 뭉뚱그리면 본인 후기가 죽는다. `-음/-함/-임`은 **평서형**이지 의문형이 아니다.
- **어휘 함정(실측)**: 맨 `짱` → 마켓명 '빈짱'에 걸림 · 맨 `래서` → 접속부사 '그래서'에 걸림 ·
  `인기`는 증거성 표지가 아님. 어휘는 `relevance_rules.LEXICON` 데이터로만 고친다.
- **전언 차단은 프롬프트로 못 한다** — 같은 입력 4회 호출에 4번 다른 답. 스키마 필드
  `firsthand_evidence` + 코드 필터(`drop_hearsay_reviews`)로 확정.
- **추출 입력의 99.4%가 고정 프롬프트**(실측). 비용 레버는 분류 정확도가 아니라 **호출 단위**(스레드 배치).
  배치는 덤으로 per-comment 경로의 '제목에서 지어낸 유령 항목'도 없앤다(실측 3~4건 → 0).

## 홍보성 vs 구매 (업계 규칙, 사람이 가르친 것)
- **구매(홍보성 아님)**: 할인·비매·서비스·세일 — 단독으론 그냥 구매 맥락.
- **홍보성**: 서포터(즈)·체험단·협찬·무상 제공·PPL — `review_class='promo'` 버킷으로 분리.
- **Why:** 순수 구매어를 게이트에서 제외해야 recall 손실 없이 오탐을 막는다. 상세 [ADR-0004](docs/adr/0004-promo-gate-llm-cascade.md).

## 1층 스펙 추출 규칙
- **제품명 = 제품 고유 해시태그에서만.** 해시태그 없는 비매품/공지글은 미출시라 저장 안 함.
- **base_combo = 풀 재료만** — 향료·비즈·규격 제외. 캡션 ` / ` 앞=풀조합, 뒤=향료.
- **mm 규격(6mm·8mm·7-9미리…)** = 알갱이 크기 표기. **디폼 전용이 아니다** — 폼볼도 mm 로
  표기한다(2026-08-06 사용자 정정, 예: '#라일락베르사유 … 7-9미리 폼볼 내장').
  따라서 mm 만 보고 `slime_type` 을 단정하지 말 것. 종류는 **캡션이 쓴 말**(디폼/폼볼/…)로 정한다.
- **beads** = 제품에 **든 것** — 비즈·토핑에 더해 **폼볼·디폼 같은 내장 알갱이도 포함**한다
  (2026-08-06 사용자 정정). `slime_type` 과 겹쳐도 중복이 아니다: `slime_type`='무슨 종류인가',
  `beads`='안에 뭐가 들었나'. 구매자는 뒤쪽을 보고 고른다. 크기 표기는 붙은 채로('7-9미리 폼볼').
  오픈 어휘 배열, 별도 product 행 금지, base_combo 에도 넣지 않음. 없으면 `[]`.
- 무시 태그: 마켓 자기이름 + 광역 슬라임어뿐. '샵/캔디' 든 고유태그(#위즈캔디샵)는 제품명.
  **제품명은 해시태그 중 '하나'이지 전부가 아니다**(2026-08-06 사용자 정정) — 판매자는 보통
  `#마켓명 #제품명` 을 함께 단다. 구현: `extract.product_hashtags(text, exclude=…)` +
  `market_tag_exclusions(KB 레코드)`. 안 거르면 마켓마다 자기 이름의 유령 제품 행이 생긴다.

## 개체연결 (linking)
- **Why:** 초성 충돌 12그룹(ㅁㅁ, ㅇㅇ, ㄴㅈ 등) 존재 → 확신도 0.6 미만이면 **abstain**.
- **Gotcha:** 추출기가 제품명 대신 종류단어(예: "촉감류")를 잡으면 조인 실패 → 약칭 사전으로 보강.
- **Note:** `index.py` 는 raw mentioned_product 가 아니라 `linking` 정규화 결과(`lk.product`)를 저장해야 함(과거 버그).
- **Important:** 제품 약칭은 **초성이 아니라 음절 클리핑**이다(`허니푸냥이`→`푸냥이`).
  초성 약칭이 통하는 건 **마켓명**뿐이다(`베이퍼`→`ㅂㅇㅍ`) — 마켓명은 짧아서.
  제품명은 6~9음절이라 초성이 길어 안전해 보이지만 실측은 반대다(2026-08-09, 디시 171스레드):
  생성 초성 매칭 14건 중 **9건 오탐** — `ㅋㅋㅋㅋㅋㅇ`(쿠키컵코코아)가 웃음 런 안에서,
  `ㅂㅋㅋㅋ`(바콕쿠키)가 `ㅅㅂㅋㅋㅋ` 안에서, `ㅅㄹㅇ`(슬랑이)가 **다른 마켓·제품의 초성**
  `ㅅㄹㅇㅂㄴ`·`ㅅㄹㅇㅈㄴ` 안에서 걸렸다(6건). 깨끗한 4건은 전부 사람이 시드한 `푸냥이`.
  **Don't:** 제품 초성을 생성하지 말 것(`linking._kb_surface_forms` 가 같은 이유로 이미 뺀다).
  약칭 재료는 `data/product_aliases.json` 사람 시드뿐이고, 틀린 시드는 본문에 안 걸려 무해하니
  점진적으로 채우면 된다(재추출 불필요).
- **Important:** 그런데 **LLM 이 약칭을 알아야 한다**(사용자 지적 2026-08-09). linking 은 모델이
  이미 뽑은 것만 정규화하므로, 모델이 `아바`를 제품으로 **인식**하지 못하면 정규화할 대상 자체가
  없다. 그래서 `extract.build_product_vocab` 이 만든 어휘를 스레드 프롬프트 머리말
  `[제품 후보]` 로 넣는다 — 단 **그 스레드 본문에 등장한 표면형만**(`vocab_candidates`),
  그리고 사후에 `enforce_product_vocab` 이 코드로 재검사한다(규칙은 프롬프트, 강제는 코드).
  실측 커버리지: 신규 조각을 가진 171스레드 중 **129개(75%)** 에 정규 제품명이 그대로 등장한다.
- **Important: amos `attributes` 에 `mentioned_market` 이 없다 → 마켓 축은 리플레이 불가.**
  실측 키(2026-08-11): `firsthand_evidence · longevity · overall · sound · texture · scent ·
  mentioned_product · shipping_cs · value`. `link()` 의 첫 입력이 저장돼 있지 않으므로
  **마켓 귀속을 재추출 없이 재계산할 방법이 없다.** 제품 축은 반대다 —
  `pipeline.dc_attribution_target` 이 순수 함수라 `evals/replay_dc_attribution_target.py` 가
  $0 로 리플레이한다.
  **Why it matters:** 그래서 마켓 축의 유일한 측정치는 **사람이 만든 전수 감사 문서**이고,
  그게 [ADR-0018](docs/adr/0018-attribution-priority-and-unregistered-markets.md) 이
  '측정 없이 건드리지 말 것'으로 봉인돼 있던 귀속 순서를 바꾼 근거다.
  **Don't:** '리플레이해서 확인하자'는 계획을 마켓 축에 세우지 말 것 — 재발견 방지용 기록이다.
- **Important: 마켓 표면형 스캔은 부분문자열이면 안 된다.** 실측(2026-08-11, amos 801행):
  `토끼나마나` 안의 `마나`(마나슬라임) · `포이즈닝` 안의 `포이`(포이슬라임)가 잡혔다 —
  둘 다 **진짜 제품명**이라 제품명이 마켓 근거로 둔갑한다. 그렇다고 양쪽 경계를 다 막으면
  `늪지에서 샀는데` 가 죽는다(조사는 이름에 붙어서 온다). 규칙: 왼쪽이 음절이면 거부,
  오른쪽은 **조사일 때만** 허용(`linking._surface_occurs`).
  그리고 `스르륵` 은 마켓명이자 의태어라 `AMBIGUOUS_SURFACES` 다(`손사이로 스르륵 나가는`).
- **Important: KB 명부가 커지면 감사 숫자가 움직인다.** 같은 검수 스크립트가 KB 14마켓에선
  D3 105 · F1 78 · 🔧 179 를, 38마켓에선 **D3 3 · F1 115 · 🔧 220** 을 낸다(판정 규칙은 불변 —
  D2·D5a·D5b·D6·D7·D8·D10 은 한 건도 안 움직였다). 미등재로 못 채우던 행이 채울 수 있는 행으로
  옮겨 갈 뿐이다.
  **Don't:** 감사 기준선을 '고정된 진실'로 인용하지 말 것 — **KB 스냅샷과 함께** 읽어야 한다.

## 수집 (합법성)
- 디시는 익명 UGC·robots 준수·발췌 표시(전문 미공개)라 가장 방어 가능한 스크랩(면접 포인트).
  원문 저장·LLM 처리는 허용, 화면에는 발췌+링크만 — [ADR-0013](docs/adr/0013-processing-vs-publication.md).
- Apify 해시태그는 공개 데이터, `meta.source=apify`/`scraped=True` 라벨링. `APIFY_TOKEN` 만 있으면 무료/제한 플랜도 동작.
- IG 공식 `business_discovery` 는 App Review 벽 → 1층은 fixture. [ADR-0003](docs/adr/0003-ig-businessdiscovery-fixture.md).

## 확정된 스택 결정 → docs/adr/
- 임베딩 BGE-M3 / 벡터스토어 pgvector → [ADR-0001](docs/adr/0001-embedding-and-vectorstore.md)
- 소스 편향은 1급 기능(평균 금지) → [ADR-0002](docs/adr/0002-source-bias-first-class.md)
- 후기(주문) 단위 vs 제품 단위 분리 → [ADR-0005](docs/adr/0005-review-vs-product-unit.md)
- 디시 마켓 귀속 우선순위 · 미등재 마켓 오버레이 · 되돌리기 센티널 · 복구 원장 →
  [ADR-0018](docs/adr/0018-attribution-priority-and-unregistered-markets.md)
