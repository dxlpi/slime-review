# --- 수집·정제 (Phase 1) ---
requests>=2.31
beautifulsoup4>=4.12
apify-client>=1.7         # 인스타 해시태그 스크래퍼(2층 유저후기) — actor run·dataset 페이지네이션

# --- LLM·관측성 (Phase 2~) ---
openai>=1.40
python-dotenv>=1.0

# --- 색인·검색 (Phase 4) ---
numpy>=1.26               # relevance.py 가 직접 import(코사인·centroid). torch 의 전이 의존에 기대지 말 것 —
                          # CI 오프라인 게이트는 torch 를 설치하지 않으므로 명시 선언이 필요하다.
torch>=2.6                # transformers 가 .bin 로드 시 요구(CVE-2025-32434). BGE-M3 가 .bin 배포라 필수.
FlagEmbedding>=1.2        # BGE-M3 임베딩(한국어 + dense/sparse)
psycopg[binary]>=3.1      # pgvector(Postgres)
pgvector>=0.3
kiwipiepy>=0.18           # 한국어 형태소 토큰화(하이브리드 키워드)
rank-bm25>=0.2            # BM25

# --- 마켓 로고 (ADR-0010) ---
Pillow>=10.0              # 아바타를 320px 로 **로컬 강제**. 실측(2026-08-06): 인스타 CDN 이 URL 의
                          # `stp=s320x320` 힌트를 무시하고 원본(최대 1080px)을 준다 — 경계를 URL 에
                          # 맡기면 판매자 원본 해상도가 커밋된다. streamlit/torch 가 전이로 끌어오지만
                          # 명시 선언한다(numpy 와 같은 이유 — CI 오프라인 게이트는 전이에 못 기댄다).

# --- UI (Phase 6) ---
streamlit>=1.58           # `st.iframe`(판매자 IG 임베드) 하한. 구버전엔 이 최상위 API 가 없어
                          # 새 배포에서 렌더 시점 AttributeError 가 난다 — 배포가 마지막 하드게이트라
                          # 하한을 실제 검증한 버전으로 올린다(`st.components.v1.iframe` 는 폐기 예정).

# --- API (Phase 6) ---
# 프런트엔드는 `web/`(Vite+React, npm)이라 여기 없다(ADR-0012). 파이썬 쪽은 그 화면이
# 백엔드를 보는 창구인 HTTP API 만 담당한다 — `api/main.py` 는 pipeline 호출 + 직렬화뿐.
fastapi>=0.115
uvicorn[standard]>=0.30
